# On the EC2 server, copy this to a file named exactly ".env" (in the same
# folder as docker-compose.prod.yml) and set a real password. Docker Compose
# automatically reads a file named ".env" and substitutes ${DB_PASSWORD} in
# docker-compose.prod.yml with this value.

DB_PASSWORD=changeme
